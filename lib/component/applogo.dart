import 'package:multichatapp/const/const.dart';

Widget applogo(){

  return const Image(image: AssetImage(icchat),color: green,).box.white.roundedSM.padding( const EdgeInsets.all(8)).size(100,100).make();
}